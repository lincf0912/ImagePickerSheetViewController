# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

project './ImagePickerSheet/ImagePickerSheet.xcodeproj'

platform :ios, '7.0'

inhibit_all_warnings!

target 'ImagePickerSheet' do
pod 'LFImagePickerController'
end
